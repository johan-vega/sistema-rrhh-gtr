# Backend GTR

El backend está en `backend/` y es una API REST Laravel 12 con autenticación Sanctum. Para instalarlo, consulte `backend/docs/backend.md`.

Comandos principales: `composer install`, configurar `.env`, `php artisan migrate --seed`, `php artisan serve` y `php artisan test` ejecutados dentro de `backend/`.
